# Руководство по разработке ControlBot

Этот документ содержит информацию для разработчиков, желающих внести вклад в проект ControlBot.

## 🏗️ Архитектура проекта

### Структура кода

```
ControlBot_v2/
├── app/                    # Основной код приложения
│   ├── config/            # Конфигурация (Pydantic Settings)
│   │   └── paths.py       # Конфигурация путей
│   ├── core/              # Основная логика
│   │   ├── centralized_handler.py  # Централизованный обработчик логирования
│   │   ├── logging.py             # Система логирования
│   │   ├── metrics_decorator.py   # Декораторы для метрик
│   │   └── security.py            # Система безопасности
│   ├── handlers/          # Обработчики команд Telegram
│   │   ├── attachments.py         # Обработка файлов и фото
│   │   ├── auth.py               # Аутентификация
│   │   ├── cancel.py             # Отмена операций
│   │   ├── cmd.py                # Выполнение команд
│   │   ├── command_search.py     # Поиск команд
│   │   ├── files.py              # Работа с файлами
│   │   ├── help.py               # Справка
│   │   ├── logs_export.py        # Экспорт логов
│   │   ├── monitor.py            # Мониторинг
│   │   ├── mouse_keyboard.py     # Мышь и клавиатура
│   │   ├── paths_handlers.py     # Управление путями
│   │   ├── processes.py          # Управление процессами
│   │   ├── registration.py       # Регистрация обработчиков
│   │   ├── remote_desktop.py     # Удаленный рабочий стол
│   │   ├── screen.py             # Скриншоты
│   │   ├── security_handlers.py  # Обработчики безопасности
│   │   ├── stats.py              # Статистика и метрики
│   │   └── system.py             # Системные команды
│   ├── middleware/        # Middleware для логирования
│   │   └── logging_middleware.py # Логирование взаимодействий с ботом
│   ├── services/          # Сервисы (жизненный цикл, мониторинг, метрики)
│   │   ├── centralized_logging.py  # Централизованное логирование
│   │   ├── lifecycle.py           # Управление жизненным циклом
│   │   ├── metrics.py             # Система метрик
│   │   ├── monitor.py             # Мониторинг файлов
│   │   └── process_stream.py      # Потоковая обработка команд
│   ├── app.py            # Главный файл приложения
│   ├── gui_utils.py      # Утилиты для GUI/headless режима
│   ├── help_texts.py     # Тексты справки
│   ├── router.py         # Роутер команд
│   └── state.py          # Состояние приложения
├── scripts/               # Платформо-специфичные скрипты
│   └── windows/          # Windows скрипты
│       └── ControlBot.bat # Запуск бота на Windows
├── tests/                 # Тесты
├── docs/                  # Документация
└── main.py               # Точка входа
```

### Основные компоненты

- **Config**: Конфигурация с Pydantic Settings (`app/config/`)
- **Core**: Основная логика приложения (`app/core/`)
  - **Logging**: Централизованная система логирования с экспортом
  - **Security**: Система безопасности с подтверждениями
  - **Metrics**: Декораторы для автоматического сбора метрик
- **Handlers**: Обработчики команд Telegram (файлы в `app/handlers/`)
  - **Registration**: Централизованная регистрация обработчиков
  - **Logs Export**: Команды экспорта логов в различные форматы
  - **Stats**: Команды статистики и метрик
- **Services**: Сервисы для управления жизненным циклом и мониторинга
  - **Centralized Logging**: Централизованное логирование
  - **Metrics**: Система сбора и анализа метрик
  - **Lifecycle**: Управление жизненным циклом приложения
- **Middleware**: Middleware для логирования взаимодействий
- **Router**: Центральный роутер для регистрации команд

## 🏛️ Новая архитектура (v2.2+)

### Принципы организации кода

1. **Разделение ответственности**:
   - `app/core/` - основная бизнес-логика (логирование, безопасность, метрики)
   - `app/config/` - конфигурация с Pydantic Settings
   - `app/handlers/` - обработчики Telegram команд
   - `app/services/` - сервисы (жизненный цикл, мониторинг, метрики)
   - `app/middleware/` - middleware для логирования

2. **Единый источник правды**:
   - Конфигурация путей в `app/config/paths.py`
   - Система безопасности в `app/core/security.py`
   - Централизованное логирование в `app/services/centralized_logging.py`
   - Система метрик в `app/services/metrics.py`

3. **Централизованное управление**:
   - Регистрация обработчиков в `app/handlers/registration.py`
   - Централизованное логирование с экспортом
   - Автоматический сбор метрик через декораторы

4. **Обратная совместимость**:
   - Старые модули сохраняют API
   - Плавная миграция без breaking changes

### Модули конфигурации

#### `app/config/paths.py`
```python
from app.config.paths import get_paths_config

config = get_paths_config()
config.add_user_path("my_app", "/path/to/app")
all_paths = config.get_all_paths()
```

#### `app/core/security.py`
```python
from app.core.security import get_confirmation_manager

manager = get_confirmation_manager()
confirmation_id = await manager.create_confirmation(
    chat_id=123,
    action_type="reload",
    action_data={"reason": "test"},
    warning_message="Test reload"
)
```

#### `app/services/centralized_logging.py`
```python
from app.services.centralized_logging import get_centralized_logger

logger = get_centralized_logger()
logger.add_log("INFO", "Test message", "test_logger")
export_path = await logger.export_to_json()
```

#### `app/services/metrics.py`
```python
from app.services.metrics import get_metrics_collector

metrics = get_metrics_collector()
metrics.record_command_execution("test_command", user_id=123, execution_time=1.5)
stats = metrics.get_command_statistics()
```

#### `app/core/metrics_decorator.py`
```python
from app.core.metrics_decorator import track_command_metrics

@track_command_metrics("my_command")
async def handle_my_command(message: Message) -> None:
    # Автоматический сбор метрик
    pass
```

### Переименованные файлы

| Старое имя | Новое имя | Причина |
|------------|-----------|---------|
| `docs_photos.py` | `attachments.py` | Более общее название |
| `find_cmd.py` | `command_search.py` | Глагол+существительное |
| `rdp.py` | `remote_desktop.py` | Более явное название |
| `security.py` | `security_handlers.py` | Разделение логики и обработчиков |
| `paths.py` | `paths_handlers.py` | Разделение логики и обработчиков |

## 🛠️ Настройка среды разработки

### Требования

- Python 3.11+
- Windows (основная платформа)
- Git

### Установка

1. **Клонируйте репозиторий**
   ```bash
   git clone https://github.com/Eklipti/TelegramControlBot.git
   cd TelegramControlBot
   ```

2. **Создайте виртуальное окружение**
   ```bash
   python -m venv .venv
   .\.venv\Scripts\activate  # Windows
   ```

3. **Установите зависимости**
   ```bash
   pip install -r requirements.txt
   ```

4. **Настройте конфигурацию**
   ```bash
   copy .env.example .env
   # Отредактируйте .env файл с вашими настройками
   ```

## ⚙️ Система конфигурации

### Pydantic Settings

ControlBot использует Pydantic Settings для управления конфигурацией. Все настройки определены в `app/config/__init__.py` и могут быть заданы через переменные окружения или `.env` файл.

### Основные настройки

#### Обязательные параметры
- `TELEGRAM_BOT_TOKEN` - токен Telegram бота (получить у @BotFather)
- `ALLOWED_USER_IDS` - список разрешенных пользователей через запятую

#### Режим отображения
- `GUI_ENABLED` - включить GUI режим (по умолчанию: true)
  - `true`/`1`/`yes` - режим с графическим интерфейсом
  - `false`/`0`/`no` - headless режим без GUI

#### Логирование
- `LOG_LEVEL` - уровень логирования (DEBUG, INFO, WARNING, ERROR, CRITICAL)

#### Пути к файлам
- `DEFAULT_PATHS_FILE` - файл с путями по умолчанию
- `LOGS_DIRECTORY` - директория для логов

#### Системные настройки
- `ENCODING` - кодировка (автоматически cp866 для Windows)

### Использование в коде

```python
from app.config import get_settings, Settings

# Получение настроек
settings = get_settings()

# Проверка пользователя
if settings.is_user_allowed(user_id):
    # пользователь разрешен

# Получение списка разрешенных пользователей
allowed_users = settings.get_allowed_user_ids()

# Создание необходимых директорий
settings.ensure_directories()

# Перезагрузка настроек
reload_settings()
```

### Валидация настроек

Система автоматически валидирует:
- Уровень логирования (только допустимые значения)
- Режимы работы (нельзя одновременно использовать GUI и headless)
- Кодировку (автоматическая установка для Windows)
- Формат списка пользователей

### Пример .env файла

```env
# Обязательные настройки
TELEGRAM_BOT_TOKEN=your_bot_token_here
ALLOWED_USER_IDS=123456789,987654321

# Режим отображения
GUI_ENABLED=true

# Логирование
LOG_LEVEL=INFO

```

## 🧪 Тестирование

### Планируемый запуск тестов

```bash
# Все тесты
python run_tests.py --all

# Только headless-тесты (для CI)
python run_tests.py --headless

# Только smoke-тесты
python run_tests.py --smoke-only

# С дополнительными аргументами pytest
python run_tests.py --smoke-only -v
python run_tests.py --headless --tb=long
python run_tests.py --all -x

# Прямой запуск pytest
python -m pytest tests/ -m smoke -q
python -m pytest tests/ -m gui -q
python -m pytest tests/ -m headless -q
```

### Планируемые маркеры тестов

- `@pytest.mark.smoke` - базовые smoke-тесты
- `@pytest.mark.unit` - unit тесты компонентов
- `@pytest.mark.headless` - тесты для headless-окружения
- `@pytest.mark.gui` - тесты, требующие GUI модули
- `@pytest.mark.integration` - интеграционные тесты

### Текущее состояние тестирования

В настоящее время в проекте отсутствуют тесты. Папка `tests/` содержит только файл `__init__.py`.

### Планы по тестированию

#### Планируемые тесты
- **`test_config.py`**: тесты конфигурации Pydantic Settings
- **`test_core_security.py`**: тесты системы безопасности
- **`test_handlers.py`**: тесты обработчиков команд
- **`test_integration.py`**: интеграционные тесты
- **`test_smoke.py`**: smoke тесты

#### Планируемые unit тесты для `app.core.security`
- `TestPrivateChatFilter` - тесты для фильтра приватных чатов
- `TestConfirmationManager` - тесты для менеджера подтверждений
- `TestDangerousActions` - тесты для конфигурации опасных действий
- `TestSecurityFunctions` - тесты для функций инициализации

#### Планируемые контракты для handlers
- `TestHandlerImports` - тесты для импорта всех handlers
- `TestHandlerContracts` - тесты для базовой функциональности
- `TestHandlerSmokeTests` - smoke тесты для handlers

#### Планируемые smoke тесты
- Тесты для `app.core.security` импорта
- Детальные проверки handlers
- Покрытие критических импортов в headless режиме

### Планируемое тестирование конфигурации

```bash
# Тесты конфигурации
python -m pytest tests/test_config.py -v

# Тесты с проверкой валидации
python -m pytest tests/test_config.py::TestSettings::test_log_level_validation -v
```

Планируется тестирование конфигурации с помощью:
- Валидации всех параметров
- Проверки значений по умолчанию
- Тестирования парсинга списка пользователей
- Проверки режимов работы

## 📝 Стиль кода

### Форматирование

Проект использует:
- **Ruff** для линтинга и форматирования
- **MyPy** для проверки типов
- **Black**-совместимое форматирование

### Запуск проверок

```bash
# Линтинг
ruff check .

# Форматирование
ruff format .

# Проверка типов
mypy app/
```

## 🔧 Добавление новых команд

### 1. Создайте обработчик

Создайте файл в `app/handlers/` или добавьте в существующий:

```python
from aiogram.filters import Command
from aiogram.types import Message
from ..router import router
from ..config import get_settings

@router.message(Command("new_command"))
async def handle_new_command(message: Message) -> None:
    settings = get_settings()
    
    # Проверка пользователя (если нужно)
    if not settings.is_user_allowed(message.from_user.id):
        await message.answer("Доступ запрещен")
        return
    
    # Использование настроек
    if not settings.gui_enabled:
        await message.answer("Команда выполняется в headless режиме")
    else:
        await message.answer("Новая команда!")
```

### 2. Добавьте справку

Обновите `app/help_texts.py`:

```python
COMMAND_HELP = {
    # ... существующие команды ...
    "new_command": {
        "description": "Описание команды",
        "detailed": "Подробная справка по команде"
    }
}
```

### 3. Обновите документацию

Добавьте команду в `docs/all_commands.md` и обновите категории в `app/handlers/help.py`.

## 🔒 Безопасность

### Принципы безопасности

1. **Whitelist-аутентификация**: Только разрешенные пользователи
2. **Приватные чаты**: Работа только в личных сообщениях
3. **Валидация входных данных**: Проверка всех пользовательских вводов
4. **Ограничения доступа**: Запрет доступа к системным директориям

### Добавление новых проверок безопасности

1. Создайте фильтр в `app/security.py`
2. Примените фильтр к роутеру в `app/app.py`
3. Добавьте тесты для проверки безопасности

## 🐛 Отладка

### Логирование

```python
import logging
logger = logging.getLogger(__name__)

# В обработчике
logger.info("Команда выполнена")
logger.error(f"Ошибка: {e}")
```

### Headless-режим

Для отладки в headless-окружении:

```bash
# Windows
set HEADLESS_MODE=1
python main.py

# Linux/Mac
export HEADLESS_MODE=1
python main.py
```

### Миграция с старой системы конфигурации

Если вы используете старую систему конфигурации (`app/config.py`), выполните миграцию:

1. **Обновите импорты**:
   ```python
   # Старый способ
   from app.config import Settings, load_env
   
   # Новый способ
   from app.config import get_settings, reload_settings
   ```

2. **Измените инициализацию**:
   ```python
   # Старый способ
   load_env()
   settings = Settings.load()
   
   # Новый способ
   settings = get_settings()
   ```

3. **Обновите проверку пользователей**:
   ```python
   # Старый способ
   from app.config import is_user_allowed
   is_user_allowed(user_id, settings.allowed_user_ids)
   
   # Новый способ
   settings.is_user_allowed(user_id)
   ```

## 📦 Сборка и развертывание

### Локальная сборка

```bash
python -m build
```

### Тестирование сборки

```bash
pip install dist/controlbot-2.0.0-py3-none-any.whl
```

## 🤝 Вклад в проект

### Процесс

1. **Fork** репозитория
2. **Создайте ветку** для новой функции
3. **Внесите изменения** с тестами
4. **Запустите тесты** и проверки
5. **Создайте Pull Request**

### Требования к PR

- [ ] Все тесты проходят
- [ ] Код соответствует стилю проекта
- [ ] Добавлены тесты для новой функциональности
- [ ] Обновлена документация
- [ ] Нет конфликтов с основной веткой

## 📚 Полезные ресурсы

- [Aiogram 3 Documentation](https://docs.aiogram.dev/)
- [Python Telegram Bot API](https://core.telegram.org/bots/api)
- [Ruff Documentation](https://docs.astral.sh/ruff/)
- [MyPy Documentation](https://mypy.readthedocs.io/)

## 🆘 Получение помощи

- **Issues**: Создайте issue для багов и предложений
- **Discussions**: Обсуждение идей и вопросов
- **Telegram**: Свяжитесь с авторами проекта

---

*Последнее обновление: 2025-09-20*
