# 📚 Справочник команд TelegramControlBot v3.1.0

Полное руководство по всем доступным командам бота TelegramControlBot.

## Содержание

- [Основные команды](#основные-команды)
- [Управление процессами](#управление-процессами)
- [Управление путями](#управление-путями)
- [Файловые операции](#файловые-операции)
- [Мониторинг файлов](#мониторинг-файлов)
- [Командная строка](#командная-строка)
- [Удаленное управление](#удаленное-управление)
- [Управление мышью](#управление-мышью)
- [Управление клавиатурой](#управление-клавиатурой)
- [Работа с экраном](#работа-с-экраном)
- [Логирование](#логирование)
- [Статистика и метрики](#статистика-и-метрики)
- [Системные команды](#системные-команды)

---

## Основные команды

### /start

**Описание:** Приветственное сообщение с информацией о боте и inline-меню навигации

**Использование:**
```
/start
```

**Что делает:**
- Показывает приветственное сообщение с описанием возможностей
- Отображает inline-меню с категориями команд (3 страницы)
- Предоставляет быструю навигацию по функциям бота

---

### /help

**Описание:** Справочная система бота

**Использование:**
```
/help              # Показать все команды по категориям
/help <команда>    # Подробная справка по конкретной команде
```

**Примеры:**
```
/help
/help on
/help mouse_move
```

---

### /cancel

**Описание:** Отмена текущей операции

**Использование:**
```
/cancel
```

**Когда использовать:**
- Отмена загрузки файла
- Прерывание ожидания ввода
- Выход из текущего состояния FSM

---

## Управление процессами

### /on

**Описание:** Запуск программы или скрипта

**Использование:**
```
/on <имя_или_путь> [аргументы]
/on hidden:<имя_или_путь> [аргументы]
/on admin:<имя_или_путь> [аргументы]
```

**Параметры:**
- `имя_или_путь` - имя из сохраненных путей или полный путь к файлу
- `аргументы` - аргументы командной строки для программы (опционально)
- `hidden:` - запуск в скрытом режиме (без окна)
- `admin:` - запуск с правами администратора (требует UAC)

**Примеры:**
```
/on notepad
/on "C:\Program Files\MyApp\app.exe"
/on hidden:script.py
/on admin:cmd.exe
/on python script.py --debug
```

**Особенности:**
- Автоматически ищет исполняемые файлы в системном PATH
- Предлагает сохранить новый путь для быстрого доступа
- Поддерживает Python скрипты и системные команды
- Режим admin требует локального подтверждения UAC

---

### /off

**Описание:** Остановка процесса

**Использование:**
```
/off                # Показать список активных процессов
/off <имя_или_PID>  # Остановить конкретный процесс
/off all            # Остановить все процессы
```

**Примеры:**
```
/off
/off notepad
/off 1234
/off all
```

**Безопасность:**
- Остановка всех процессов требует подтверждения
- Процесс завершается принудительно

---

### /processes

**Описание:** Показать активные процессы, запущенные ботом

**Использование:**
```
/processes
```

**Информация:**
- Имя процесса
- Статус (активен/завершен)
- PID (идентификатор процесса)
- Полный путь к исполняемому файлу

---

### /reload

**Описание:** Перезагрузка системы

**Использование:**
```
/reload
```

**Предупреждение:**
- Немедленная перезагрузка компьютера!
- Требует подтверждения
- Несохраненные данные будут потеряны

---

## Управление путями

### /path_global_list

**Описание:** Показать системные пути из PATH

**Использование:**
```
/path_global_list
```

**Особенности:**
- Показывает все пути из системной переменной PATH
- Доступны всем пользователям бота
- Только для просмотра (нельзя изменять)

---

### /path_user_add

**Описание:** Добавить пользовательский путь

**Использование:**
```
/path_user_add <имя> <путь>
```

**Параметры:**
- `имя` - уникальное имя для пути
- `путь` - абсолютный путь к файлу или директории

**Примеры:**
```
/path_user_add MyApp "C:\Program Files\MyApp\app.exe"
/path_user_add notepad++ "C:\Program Files\Notepad++\notepad++.exe"
```

**Особенности:**
- Сохраняется в `jsons/{user_id}/{user_id}_paths.json`
- Доступен только текущему пользователю
- Имеет приоритет над системными путями

---

### /path_user_list

**Описание:** Показать пользовательские пути

**Использование:**
```
/path_user_list
```

**Информация:**
- Имя пути
- Полный путь к файлу
- Статус (существует/не найден)

---

### /path_user_del

**Описание:** Удалить пользовательский путь

**Использование:**
```
/path_user_del <имя>
```

**Пример:**
```
/path_user_del MyApp
```

---

### /paths_show_all

**Описание:** Показать все доступные пути

**Использование:**
```
/paths_show_all
```

**Что показывает:**
- Пользовательские пути (приоритет)
- Системные пути (из PATH)
- Объединенный список всех путей

---

### /paths_reload

**Описание:** Перезагрузить все пути

**Использование:**
```
/paths_reload
```

**Что делает:**
- Перезагружает системные пути из PATH
- Перезагружает пользовательские пути
- Показывает статистику загрузки

**Когда использовать:**
- После изменения системного PATH
- При проблемах с путями
- Для обновления без перезапуска бота

---

## Файловые операции

### /upload

**Описание:** Загрузить файл на компьютер

**Использование:**
```
/upload <путь>
```

**Параметры:**
- `путь` - путь для сохранения файла или директория

**Процесс:**
1. Отправьте команду с путем
2. Отправьте файл боту
3. Файл будет сохранен по указанному пути

**Примеры:**
```
/upload C:\Downloads\file.txt
/upload D:\Projects\
```

**Особенности:**
- Если путь - директория, сохраняет с оригинальным именем
- Автоматически создает недостающие директории
- Используйте `/cancel` для отмены

---

### /download

**Описание:** Скачать файл с компьютера

**Использование:**
```
/download <путь>
```

**Примеры:**
```
/download C:\file.txt
/download D:\Projects\report.pdf
```

**Особенности:**
- Поддерживает файлы до 50 МБ (ограничение Telegram)
- Папки автоматически архивируются в ZIP

---

### /cut

**Описание:** Скачать файл и удалить его

**Использование:**
```
/cut <путь>
```

**Примеры:**
```
/cut C:\temp\file.txt
```

**Предупреждение:**
- Файл будет удален после скачивания
- Операция необратима

---

### /find

**Описание:** Поиск файлов на компьютере

**Использование:**
```
/find name:<шаблон> [size:<фильтр>] [ext:<расширения>]
```

**Параметры:**
- `name:` - шаблон имени файла (поддерживает * и ?)
- `size:` - фильтр по размеру (+100M, -1G)
- `ext:` - расширения файлов через запятую

**Примеры:**
```
/find name:report*
/find name:*.py size:+1M
/find name:document* ext:docx,pdf size:-10M
```

**Особенности:**
- Результаты отправляются файлом при большом количестве
- Поддерживает рекурсивный поиск

---

## Мониторинг файлов

### /monitor_add

**Описание:** Добавить путь для мониторинга изменений

**Использование:**
```
/monitor_add <путь>
```

**Примеры:**
```
/monitor_add C:\Important\Documents
/monitor_add D:\Projects\my_app
```

**Возможности:**
- Отслеживание создания новых файлов
- Обнаружение изменений существующих файлов
- Выявление удаления файлов
- Мониторинг переименования файлов

**Типы уведомлений:**
- 📝 Изменение содержимого файла
- 📁 Создание нового файла/папки
- 🗑️ Удаление файла/папки
- 🔄 Переименование файла/папки

---

### /monitor_remove

**Описание:** Удалить путь из мониторинга

**Использование:**
```
/monitor_remove <путь>
```

**Примеры:**
```
/monitor_remove C:\Important\Documents
```

---

### /monitor_list

**Описание:** Показать все активные мониторинги

**Использование:**
```
/monitor_list
```

**Информация:**
- Полный путь к отслеживаемой папке/файлу
- Статус мониторинга
- Количество отслеживаемых объектов

---

### /monitor_stop

**Описание:** Остановить все мониторинги

**Использование:**
```
/monitor_stop
```

**Предупреждение:**
- Немедленная остановка всех мониторингов
- Требует подтверждения
- Операция необратима

---

## Командная строка

### /cmd_session_start

**Описание:** Запуск интерактивной cmd сессии

**Использование:**
```
/cmd_session_start [команда]
```

**Примеры:**
```
/cmd_session_start
/cmd_session_start ipconfig
```

**Особенности:**
- Создает новую интерактивную cmd сессию
- Показывает статус выполнения, время и количество строк
- Включает inline-кнопку "🔄 Обновить"
- Автоматически обновляет отображение каждые 0.25 секунды

---

### /cmd

**Описание:** Отправка команды в активную сессию

**Использование:**
```
/cmd <команда>
```

**Примеры:**
```
/cmd dir
/cmd ipconfig /all
/cmd echo Hello World
```

**Требования:**
- Требует предварительного запуска сессии через `/cmd_session_start`

---

### /cmdupdate

**Описание:** Принудительное обновление отображения сессии

**Использование:**
```
/cmdupdate
```

**Особенности:**
- Мгновенно обновляет сообщение без ожидания таймера
- Также доступно через inline-кнопку "🔄 Обновить"

---

### /cmd_dump

**Описание:** Отправить полный вывод сессии файлом

**Использование:**
```
/cmd_dump
```

**Особенности:**
- Включает весь накопленный вывод без обрезки
- Файл содержит полный вывод с временной меткой
- Автоматически удаляется после отправки

---

### /cmd_session_stop

**Описание:** Остановка cmd сессии

**Использование:**
```
/cmd_session_stop
```

**Что делает:**
- Завершает активную cmd сессию
- Останавливает процесс командной строки
- Очищает сессию из памяти

---

## Удаленное управление

### /rdp_start

**Описание:** Запуск трансляции экрана (RDP)

**Использование:**
```
/rdp_start [fps]
```

**Параметры:**
- `fps` - частота кадров (1-10), по умолчанию 1

**Примеры:**
```
/rdp_start
/rdp_start 3
/rdp_start 5
```

**Особенности:**
- Редактирует одно сообщение для экономии места
- Автоматически останавливается при неактивности
- Требует подтверждения (опасное действие)

---

### /rdp_stop

**Описание:** Остановка трансляции экрана

**Использование:**
```
/rdp_stop
```

---

## Управление мышью

### /mouse_move

**Описание:** Переместить мышь в абсолютные координаты

**Использование:**
```
/mouse_move <x> <y>
```

**Примеры:**
```
/mouse_move 500 300
/mouse_move 1920 1080
```

---

### /mouse_move_rel

**Описание:** Переместить мышь относительно текущей позиции

**Использование:**
```
/mouse_move_rel <dx> <dy>
```

**Примеры:**
```
/mouse_move_rel 50 -20
/mouse_move_rel -100 100
```

---

### /mouse_save

**Описание:** Сохранить текущую позицию мыши

**Использование:**
```
/mouse_save <имя>
```

**Примеры:**
```
/mouse_save button1
/mouse_save start_menu
```

---

### /mouse_goto

**Описание:** Переместить мышь в сохраненную позицию

**Использование:**
```
/mouse_goto <имя>
```

**Примеры:**
```
/mouse_goto button1
/mouse_goto start_menu
```

**Особенности:**
- Поддерживает позиции, найденные через `/screen_find`

---

### /mouse_speed

**Описание:** Установить скорость перемещения мыши

**Использование:**
```
/mouse_speed <секунды>
```

**Примеры:**
```
/mouse_speed 0.5
/mouse_speed 1.0
```

---

### /mouse_click

**Описание:** Клик мышью

**Использование:**
```
/mouse_click [кнопка]
```

**Параметры:**
- `кнопка` - left (по умолчанию), right, middle

**Примеры:**
```
/mouse_click
/mouse_click right
/mouse_click middle
```

---

### /mouse_scroll

**Описание:** Прокрутить колесо мыши

**Использование:**
```
/mouse_scroll <шаги>
```

**Примеры:**
```
/mouse_scroll -10  # Прокрутка вверх
/mouse_scroll 5    # Прокрутка вниз
```

---

## Управление клавиатурой

### /key

**Описание:** Нажать клавишу или комбинацию клавиш

**Использование:**
```
/key <клавиша>
/key <клав1>+<клав2>+...
```

**Примеры:**
```
/key enter
/key ctrl+c
/key ctrl+alt+del
/key win+r
```

---

### /type

**Описание:** Ввести текст

**Использование:**
```
/type <текст>
```

**Примеры:**
```
/type Hello World!
/type notepad
```

---

## Работа с экраном

### /screen

**Описание:** Сделать скриншот экрана

**Использование:**
```
/screen
```

**Особенности:**
- Захватывает весь экран
- Отправляет изображением

---

### /screen_find

**Описание:** Поиск изображения на экране

**Использование:**
```
/screen_find
# Затем отправьте изображение-образец
```

**Процесс:**
1. Отправьте команду `/screen_find`
2. Отправьте изображение-образец
3. Бот найдет это изображение на экране

**Особенности:**
- Использует OpenCV template matching
- Находит координаты центра найденного объекта
- Сохраняет позицию для использования с `/mouse_goto`

---

### /screen_mark

**Описание:** Скриншот с координатной сеткой

**Использование:**
```
/screen_mark
```

**Особенности:**
- Накладывает координатную сетку на скриншот
- Помогает определить точные координаты для мыши

---

## Логирование

### /logs_export

**Описание:** Меню экспорта логов

**Использование:**
```
/logs_export
```

**Доступные форматы:**
- `/logs_export_json` - JSON формат
- `/logs_export_csv` - CSV для Excel
- `/logs_export_xml` - XML формат
- `/logs_export_txt` - текстовый файл

---

### /logs_export_json

**Описание:** Экспорт логов в JSON

**Использование:**
```
/logs_export_json
```

---

### /logs_export_csv

**Описание:** Экспорт логов в CSV

**Использование:**
```
/logs_export_csv
```

---

### /logs_export_xml

**Описание:** Экспорт логов в XML

**Использование:**
```
/logs_export_xml
```

---

### /logs_export_txt

**Описание:** Экспорт логов в текст

**Использование:**
```
/logs_export_txt
```

---

### /logs_filter

**Описание:** Фильтрация и экспорт логов

**Использование:**
```
/logs_filter [опции]
```

**Опции:**
- `--level ERROR` - фильтр по уровню
- `--logger auth` - фильтр по модулю
- `--hours 24` - за последние N часов
- `--limit 100` - максимум записей

**Примеры:**
```
/logs_filter --level ERROR --hours 1
/logs_filter --logger auth --limit 50
```

---

### /logs_cleanup

**Описание:** Очистка старых файлов экспорта

**Использование:**
```
/logs_cleanup [дни]
```

**Примеры:**
```
/logs_cleanup     # По умолчанию 30 дней
/logs_cleanup 7   # Удалить файлы старше недели
```

---

## Статистика и метрики

### /stats

**Описание:** Общая статистика бота

**Использование:**
```
/stats
```

**Информация:**
- Время работы бота
- Количество активных пользователей и сессий
- Общее количество выполненных команд
- Метрики производительности
- Топ-5 наиболее используемых команд

---

### /stats_commands

**Описание:** Детальная статистика по командам

**Использование:**
```
/stats_commands
```

**Информация:**
- Количество вызовов каждой команды
- Среднее время выполнения
- Количество ошибок

---

### /stats_users

**Описание:** Статистика по пользователям

**Использование:**
```
/stats_users
```

**Информация:**
- Список активных пользователей
- Количество команд от каждого
- Последняя активность

---

### /stats_performance

**Описание:** Метрики производительности

**Использование:**
```
/stats_performance
```

**Информация:**
- Среднее время ответа
- Использование памяти
- Количество ошибок

---

### /stats_patterns

**Описание:** Паттерны использования

**Использование:**
```
/stats_patterns
```

**Информация:**
- Часы пиковой активности
- Наиболее частые последовательности команд

---

### /stats_audit

**Описание:** Аудит-трейлы операций

**Использование:**
```
/stats_audit
```

**Информация:**
- История выполненных команд
- Пользователь и время
- Результат выполнения

---

### /stats_export

**Описание:** Экспорт статистики в JSON

**Использование:**
```
/stats_export
```

---

## Системные команды

### /tasklist

**Описание:** Список системных процессов

**Использование:**
```
/tasklist
```

**Информация:**
- Топ процессов по использованию памяти
- PID и имя процесса
- Использование ресурсов

---

## Inline-меню навигация

Начиная с версии 3.1.0, бот поддерживает удобную навигацию через inline-кнопки:

### Главное меню

Команда `/start` открывает многостраничное меню:

**Страница 1:**
- Процессы
- Пути
- Файлы
- Мониторинг
- Далее (1/3)

**Страница 2:**
- Командная строка
- Удаленное управление
- Прочее
- Далее (2/3)

**Страница 3:**
- Мышь
- Клавиатура
- Экран
- К началу (3/3)

### Навигация по категориям

При выборе категории отображается:
- Список всех команд категории в `<code>` тегах для копирования
- Inline-кнопки для каждой команды
- Кнопка "Справка по категории" для полной документации

### Обратная совместимость

Все команды продолжают работать через прямой ввод в чат!

---

**Версия документации: 3.1.0**  
**Последнее обновление: 2025-11-11**

## 📄 Лицензия

Проект распространяется по лицензии [GNU GPL v3.0](../LICENSE).

Изначальная задумка: Nlan_Cat

Автор: Eklipti

---

Copyright (C) 2025-2026 Eklipti.