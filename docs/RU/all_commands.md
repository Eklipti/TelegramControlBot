# 📚 Справочник команд ControlBot

Этот документ содержит подробное описание всех доступных команд бота ControlBot.

### Автоматическая регистрация команд
При запуске бота все команды автоматически регистрируются в меню Telegram, что обеспечивает:
- **Удобный доступ** - все команды видны в меню бота
- **Автодополнение** - Telegram предлагает команды при вводе
- **Описания команд** - каждая команда имеет краткое описание

### Уведомления о состоянии
Бот информирует пользователей о своем состоянии:
- **🟢 Бот запущен** - отправляется всем разрешенным пользователям при старте
- **⛔ Бот остановлен** - отправляется при остановке бота
- **Мгновенная обратная связь** - пользователь сразу понимает, что бот работает

### Хелп-команды
- **Команда `/start`** - приветствие с описанием возможностей
- **Команда `/help`** - полный список команд с категориями
- **Контекстная справка** - `/help <команда>` для детальной информации

## Категории команд

### Общие команды
- [`/start`](#start) - Приветствие и информация о боте
- [`/help`](#help) - Показать справку по командам

### Процессы и система
- [`/on`](#on) - Запуск программы или скрипта
- [`/off`](#off) - Остановка процесса
- [`/processes`](#processes) - Активные процессы бота
- [`/tasklist`](#tasklist) - Системные процессы (по памяти)
- [`/reload`](#reload) - Перезагрузка системы

### Пути (быстрый запуск)
- [`/add_path`](#add_path) - Добавить новый путь в конфиг
- [`/list_paths`](#list_paths) - Показать все зарегистрированные пути
- [`/del_path`](#del_path) - Удалить путь из конфига
- [`/add_default_path`](#add_default_path) - Добавить путь в предустановленные
- [`/list_default_paths`](#list_default_paths) - Показать предустановленные пути
- [`/del_default_path`](#del_default_path) - Удалить предустановленный путь
- [`/reload_paths`](#reload_paths) - Перезагрузить все пути

### Командная строка (интерактив)
- [`/cmd_session_start`](#cmd_session_start) - Запуск cmd сессии
- [`/cmd_session_stop`](#cmd_session_stop) - Остановка cmd сессии
- [`/cmd`](#cmd) - Отправка команды в сессию
- [`/cmd_wait`](#cmd_wait) - Команда с ожиданием обновления
- [`/cmdupdate`](#cmdupdate) - Мгновенное обновление отображения сессии
- [`/cmd_dump`](#cmd_dump) - Отправить полный вывод сессии файлом

### Файлы
- [`/upload`](#upload) - Загрузить файл на компьютер
- [`/download`](#download) - Скачать файл
- [`/cut`](#cut) - Скачать и удалить
- [`/find`](#find) - Поиск файлов

### Мышь и клавиатура
- [`/mouse_move`](#mouse_move) - Переместить мышь
- [`/mouse_move_rel`](#mouse_move_rel) - Переместить относительно
- [`/mouse_save`](#mouse_save) - Сохранить позицию
- [`/mouse_goto`](#mouse_goto) - Переместить в сохраненную позицию
- [`/mouse_speed`](#mouse_speed) - Скорость мыши
- [`/mouse_click`](#mouse_click) - Клик мышью
- [`/mouse_scroll`](#mouse_scroll) - Прокрутить колесо
- [`/key`](#key) - Нажать клавишу
- [`/type`](#type) - Ввести текст

### Экран
- [`/screen`](#screen) - Скриншот экрана
- [`/screen_find`](#screen_find) - Поиск изображения на экране
- [`/screen_mark`](#screen_mark) - Скриншот с координатной сеткой

### Мониторинг
- [`/monitor_add`](#monitor_add) - Добавить путь для мониторинга изменений файлов
- [`/monitor_remove`](#monitor_remove) - Удалить путь из списка мониторинга
- [`/monitor_list`](#monitor_list) - Показать все активные мониторинги
- [`/monitor_stop`](#monitor_stop) - Полная остановка всех мониторингов

### RDP (трансляция)
- [`/rdp_start`](#rdp_start) - Запуск трансляции экрана
- [`/rdp_stop`](#rdp_stop) - Остановка трансляции экрана

### Логирование и экспорт
- [`/logs_export`](#logs_export) - Меню экспорта логов
- [`/logs_export_json`](#logs_export_json) - Экспорт логов в JSON
- [`/logs_export_csv`](#logs_export_csv) - Экспорт логов в CSV
- [`/logs_export_xml`](#logs_export_xml) - Экспорт логов в XML
- [`/logs_export_txt`](#logs_export_txt) - Экспорт логов в TXT
- [`/logs_filter`](#logs_filter) - Фильтрация логов
- [`/logs_cleanup`](#logs_cleanup) - Очистка старых файлов экспорта

### Статистика и метрики
- [`/stats`](#stats) - Общая статистика бота
- [`/stats_commands`](#stats_commands) - Статистика команд
- [`/stats_users`](#stats_users) - Статистика пользователей
- [`/stats_performance`](#stats_performance) - Метрики производительности
- [`/stats_patterns`](#stats_patterns) - Паттерны использования
- [`/stats_audit`](#stats_audit) - Аудит-трейлы
- [`/stats_export`](#stats_export) - Экспорт статистики

### Прочее
- [`/cancel`](#cancel) - Отменить операцию

---

## Подробные описания

<a id="start"></a>
### `/start` - Приветствие и информация о боте

**Использование:**
```
/start
```

**Что делает:**
Показывает приветственное сообщение с основными возможностями бота и инструкциями по использованию

**Особенности:**
- Автоматически вызывается при первом запуске
- Содержит информацию о безопасности
- Рекомендует использовать `/help` для справки

**UX особенности:**
- При запуске бота все команды автоматически регистрируются в меню Telegram
- Разрешенные пользователи получают уведомление "🟢 Бот запущен" при старте
- При остановке бота пользователи получают уведомление "⛔ Бот остановлен"
- Пользователь сразу понимает, что бот работает и готов к использованию

---

<a id="help"></a>
### `/help` - Показать справку по командам

**Использование:**
```
/help - показать общий список команд
/help [команда] - детальная справка по команде
```

**Примеры:**
```
/help
/help on
/help mouse
```

---

<a id="on"></a>
### `/on` - Запуск программы или скрипта

**Использование:**
```
/on [флаги] [имя_или_путь] [аргументы]
```

**Флаги:**
- `hidden:` - скрытый режим
- `admin:` - права администратора

**Особенности:**
- Автоматически предлагает сохранить новый путь
- Поддерживает зарегистрированные имена и абсолютные пути
- Ищет исполняемые файлы в системном PATH
- Режим запуска с правами администратора на Windows требует ручного подтверждения, корректная работа только на Linux

**Примеры:**
```
/on EvilBot
/on "C:/my bot.py" --debug
/on hidden:EvilBot_ALT
/on admin:cmd.exe
/on notepad.exe C:\file.txt
```

---

<a id="add_path"></a>
### `/add_path` - Добавить новый путь в конфиг

**Использование:**
```
/add_path <имя> <путь>
```

**Параметры:**
- **имя** - уникальное имя для пути
- **путь** - абсолютный путь к файлу/директории

**Пример:**
```
/add_path MyApp "C:\Program Files\MyApp\app.exe"
```

---

<a id="list_paths"></a>
### `/list_paths` - Показать все зарегистрированные пути

**Использование:**
```
/list_paths
```

Выводит список всех сохраненных путей в формате:
```
Имя → Путь
```

---

<a id="del_path"></a>
### `/del_path` - Удалить путь из конфига

**Использование:**
```
/del_path <имя>
```

**Пример:**
```
/del_path MyApp
```

⚠️ Удаляет только из конфига, не затрагивает файлы

----

<a id="add_default_path"></a>
### `/add_default_path` - Добавить путь в предустановленные

**Использование:**
```
/add_default_path <имя> <путь>
```

**Что делает:**
- Добавляет путь в jsons/DEFAULT_PATHS.json
- Доступен всем пользователям бота
- Приоритет над предустановленными путями

**Особенности:**
- Сохраняется в jsons/DEFAULT_PATHS.json
- Влияет на всех пользователей
- Поддерживает абсолютные пути

**Пример:**
```
/add_default_path MyApp "C:\Program Files\MyApp\app.exe"
```

----

<a id="list_default_paths"></a>
### `/list_default_paths` - Показать предустановленные пути

**Использование:**
```
/list_default_paths
```

**Что показывает:**
- Все пути из jsons/DEFAULT_PATHS.json
- Поддержка массивов путей (несколько вариантов)
- Отдельно от пользовательских путей

**Особенности:**
- Показывает предустановленные пути
- Включает системные и общие пути
- Обновляется в реальном времени

----

<a id="del_default_path"></a>
### `/del_default_path` - Удалить предустановленный путь

**Использование:**
```
/del_default_path <имя>
```

**Пример:**
```
/del_default_path MyApp
```

**Внимание:**
- Удаляет из jsons/DEFAULT_PATHS.json
- Влияет на всех пользователей бота
- Не затрагивает файлы на диске

----

<a id="reload_paths"></a>
### `/reload_paths` - Перезагрузить все пути

**Использование:**
```
/reload_paths
```

**Что делает:**
- Перезагружает пути из всех файлов
- Объединяет пользовательские и предустановленные
- Показывает статистику загрузки

**Когда использовать:**
- После изменения файлов вручную
- При проблемах с путями
- Для обновления без перезапуска бота

**Пример вывода:**
```
🔄 Пути перезагружены:
• Пользовательские: 5
• Предустановленные: 8
• Всего: 13
```

---

<a id="off"></a>
### `/off` - Остановка процесса

**Использование:**
```
/off - список процессов
/off [имя_или_PID] - остановить
/off all - остановить все
```

**Примеры:**
```
/off
/off EvilBot
/off all
```

---

<a id="processes"></a>
### `/processes` - Активные процессы бота

**Использование:**
```
/processes
```

**Что показывает:**
- Процессы, запущенные через бота (из `active_processes`)
- Только те процессы, которые были запущены командой `/on`
- Статус каждого процесса (активен/завершен)

---

<a id="tasklist"></a>
### `/tasklist` - Системные процессы (по памяти)

**Использование:**
```
/tasklist
```

**Что показывает:**
- Все системные процессы, отсортированные по использованию памяти
- Не только процессы бота, а все процессы системы
- Суммарная информация по потреблению ресурсов

---

<a id="reload"></a>
### `/reload` - Перезагрузка системы

**Использование:**
```
/reload
```

⚠️ Немедленная перезагрузка!

---

<a id="screen"></a>
### `/screen` - Скриншот экрана

**Использование:**
```
/screen
```

Скриншот всего экрана

---

<a id="cmd_session_start"></a>
### `/cmd_session_start` - Запуск cmd сессии

**Использование:**
```
/cmd_session_start - запустить новую сессию
/cmd_session_start [команда] - запустить и выполнить команду
```

**Особенности:**
- Создает новую интерактивную cmd сессию
- Показывает статус выполнения, время и количество строк
- Включает inline-кнопку "🔄 Обновить" для ручного обновления
- Автоматически обновляет отображение каждые 0.25 секунды

**Примеры:**
```
/cmd_session_start
/cmd_session_start ipconfig
```

---

<a id="cmd_session_stop"></a>
### `/cmd_session_stop` - Остановка cmd сессии

**Использование:**
```
/cmd_session_stop
```

**Что делает:**
- Завершает активную cmd сессию
- Останавливает процесс командной строки
- Очищает сессию из памяти

**Особенности:**
- Работает только при наличии активной сессии
- Немедленно завершает процесс
- Освобождает ресурсы

---

<a id="cmd"></a>
### `/cmd` - Отправка команды в сессию

**Использование:**
```
/cmd [команда]
```

**Что делает:**
- Отправляет команду в активную cmd сессию
- Работает только при наличии запущенной сессии

**Особенности:**
- Требует предварительного запуска сессии через `/cmd_session_start`
- Поддерживает любые команды командной строки
- Показывает подтверждение отправки команды

**Примеры:**
```
/cmd dir
/cmd echo Hello World
/cmd ipconfig /all
```

**Управление сессией:**
- `/cmd_session_start` - запустить сессию
- `/cmd_session_stop` - остановить сессию
- `/cmd_wait` - команда с ожиданием
- `/cmdupdate` - обновить отображение
- Inline-кнопка "🔄 Обновить" - быстрое обновление

---

<a id="cmd_wait"></a>
### `/cmd_wait` - Настройка интервала автообновления

**Использование:**
```
/cmd_wait [интервал_секунды] [команда]
```

**Что делает:**
- Устанавливает интервал автообновления для cmd сессии
- При наличии команды - выполняет её
- Настройка сохраняется до завершения сессии

**Параметры:**
- **интервал_секунды** - интервал автообновления (0.25-60 секунд)
- **команда** - команда для выполнения (необязательно)

**Особенности:**
- Минимальный интервал: 0.25 секунды
- Максимальный интервал: 60 секунд
- Настройка действует только для текущей сессии
- По умолчанию: 0.25 секунды

**Примеры:**
```
/cmd_wait 1.0 - установить обновление каждую секунду
/cmd_wait 0.5 dir - обновление каждые 0.5 сек + выполнить dir
/cmd_wait 2.0 npm install - обновление каждые 2 сек + установка
```

**Когда использовать:**
- Для быстрых команд: уменьшить интервал (0.25-0.5 сек)
- Для медленных команд: увеличить интервал (1-5 сек)
- Для экономии ресурсов: большой интервал (10-60 сек)

---

<a id="cmdupdate"></a>
### `/cmdupdate` - Мгновенное обновление отображения сессии

**Использование:**
```
/cmdupdate
```

**Что делает:**
- Мгновенно обновляет отображение активной cmd сессии
- Использует принудительное обновление без ожидания таймера
- Полезно когда автоматическое обновление не сработало

**Особенности:**
- Работает только при наличии активной cmd сессии
- Также доступно через inline-кнопку "🔄 Обновить" в сообщениях cmd
- Не создает новую сессию, только обновляет существующую
- Обрезка вывода теперь происходит по строкам (последние 50 строк), а не по символам

**Пример:**
```
/cmdupdate
```

----

<a id="cmd_dump"></a>
### `/cmd_dump` - Отправить полный вывод сессии файлом

**Использование:**
```
/cmd_dump
```

**Что делает:**
- Отправляет полный вывод cmd сессии в виде текстового файла
- Включает весь накопленный вывод без обрезки
- Полезно для сохранения длинного вывода команд

**Особенности:**
- Работает только при наличии активной cmd сессии
- Файл содержит полный вывод с временной меткой
- Автоматически удаляется после отправки
- Позволяет сохранить всю историю команд без потерь

**Пример использования:**
```
1. Запустите cmd сессию: /cmd_session_start
2. Выполните команды: /cmd dir
3. Получите полный вывод: /cmd_dump
```

---

<a id="upload"></a>
### `/upload` - Загрузить файл на компьютер

**Использование:**
```
/upload [путь]
```

**Особенности:**
- Если путь - директория, сохраняет с оригинальным именем
- Автоматически создает недостающие директории

**Примеры:**
```
/upload C:\file.txt
/upload C:\Downloads
```

**Процесс:**
1. Отправьте команду с путем
2. Отправьте файл боту
3. Используйте /cancel для отмены

---

<a id="download"></a>
### `/download` - Скачать файл/папку

**Использование:**
```
/download [путь]
```

**Особенности:**
- Поддерживает скачивание как файлов, так и папок (архивирует на лету)
- Для папок требуется подтверждение
- Ограничения по размеру и количеству элементов

**Ограничения безопасности:**
- **Запрещены системные директории:** Windows, Program Files, System32 и др.
- **Максимальный размер файла:** 100 MB
- **Максимальный размер папки:** 500 MB
- **Максимум элементов в папке:** 10,000

**Примеры:**
```
/download C:\Users\User\Documents\file.txt
/download C:\Users\User\Desktop\MyFolder
```

**Процесс для папок:**
1. Отправьте команду с путем к папке
2. Подтвердите скачивание в диалоге
3. Получите ZIP-архив папки

---

<a id="cut"></a>
### `/cut` - Скачать и удалить

**Использование:**
```
/cut [путь]
```

**Пример:**
```
/cut C:\file.txt
```

---

<a id="find"></a>
### `/find` - Поиск файлов

**Использование:**
```
/find [параметры]
```

**Параметры поиска:**
- `name:` шаблон имени файла
- `size:` фильтр по размеру (+100M, -1G)
- `ext:` расширения через запятую
- `root:` корневая директория поиска
- `limit:` максимум результатов (1-10000)
- `timeout:` таймаут в секундах (5-600)

**Примеры:**
```
/find name:*.txt
/find name:report* ext:docx,pdf
/find root:C:\ size:+1M limit:500
```

**Результат:**
- Сначала показывает прогресс поиска
- Потом краткие результаты в сообщении
- Детальный отчет отправляется файлом

**Особенности:**
- На Windows использует `where` команду
- На *nix требует указания `root:`
- Автоматический таймаут и лимиты для защиты

---

<a id="mouse_move"></a>
### `/mouse_move` - Переместить мышь

**Использование:**
```
/mouse_move [x] [y]
```

**Пример:**
```
/mouse_move 500 300
```

---

<a id="mouse_move_rel"></a>
### `/mouse_move_rel` - Переместить относительно

**Использование:**
```
/mouse_move_rel [dx] [dy]
```

**Пример:**
```
/mouse_move_rel 50 -20
```

---

<a id="mouse_save"></a>
### `/mouse_save` - Сохранить позицию

**Использование:**
```
/mouse_save [имя]
```

**Пример:**
```
/mouse_save button1
```

---

<a id="mouse_goto"></a>
### `/mouse_goto` - Переместить в сохраненную позицию

**Использование:**
```
/mouse_goto [имя]
```

**Источники позиций:**
- Сохраненные с помощью `/mouse_save`
- Найденные с помощью `/screen_find` (позиция 'found')

**Примеры:**
```
/mouse_goto button1
/mouse_goto found
```

**Особенности:**
- Показывает список доступных позиций при ошибке

---

<a id="mouse_speed"></a>
### `/mouse_speed` - Скорость мыши

**Использование:**
```
/mouse_speed [секунды]
```

**Пример:**
```
/mouse_speed 0.5
```

---

<a id="mouse_click"></a>
### `/mouse_click` - Клик мышью

**Использование:**
```
/mouse_click [кнопка]
```

**Кнопки:** left, right, middle

**Пример:**
```
/mouse_click right
```

---

<a id="mouse_scroll"></a>
### `/mouse_scroll` - Прокрутить колесо

**Использование:**
```
/mouse_scroll [шаги]
```

**Пример:**
```
/mouse_scroll -10
```

---

<a id="key"></a>
### `/key` - Нажать клавишу

**Использование:**
```
/key [клавиша]
/key [клав1]+[клав2]+...
```

**Примеры:**
```
/key enter
/key ctrl+c
```

---

<a id="type"></a>
### `/type` - Ввести текст

**Использование:**
```
/type [текст]
```

**Пример:**
```
/type Hello World!
```

---

<a id="screen_find"></a>
### `/screen_find` - Поиск изображения на экране

**Использование:**
```
/screen_find
```

**Как работает:**
1. Отправьте команду `/screen_find`
2. Отправьте изображение-образец
3. Бот найдет это изображение на экране

**Особенности:**
- Использует OpenCV template matching
- Находит координаты центра найденного объекта
- Сохраняет позицию для использования с `/mouse_goto`

**Пример:**
```
/screen_find → отправьте фото кнопки
```

---

<a id="screen_mark"></a>
### `/screen_mark` - Скриншот с координатной сеткой

**Использование:**
```
/screen_mark
```

**Что показывает:**
- Координатную сетку каждые 100 пикселей
- Текущую позицию курсора мыши
- Числовые координаты по осям X и Y

**Полезно для:**
- Определения координат для `/mouse_move`
- Визуального ориентирования на экране

---

<a id="cancel"></a>
### `/cancel` - Отменить операцию

**Использование:**
```
/cancel
```

---

<a id="monitor_add"></a>
### `/monitor_add` - Добавить путь для мониторинга изменений файлов

**Использование:**
```
/monitor_add [путь_к_файлу_или_папке]
```

**Возможности мониторинга:**
- Отслеживание создания новых файлов
- Обнаружение изменений существующих файлов
- Выявление удаления файлов
- Мониторинг переименования файлов

**Типы уведомлений:**
- 📝 Изменение содержимого файла
- 📁 Создание нового файла/папки
- 🗑️ Удаление файла/папки
- 🔄 Переименование файла/папки

**Примеры:**
```
/monitor_add C:\Important\Documents
/monitor_add D:\Projects\my_app
/monitor_add /home/user/config
```

**Особенности:**
- Поддерживается рекурсивный мониторинг вложенных папок
- Отслеживаются все типы файлов
- Уведомления приходят в реальном времени
- Работает даже с сетевыми и скрытыми файлами

---

<a id="monitor_remove"></a>
### `/monitor_remove` - Удалить путь из списка мониторинга

**Использование:**
```
/monitor_remove [путь_к_файлу_или_папке]
```

**Примеры:**
```
/monitor_remove C:\Important\Documents
/monitor_remove D:\Projects\my_app
```

**Примечания:**
- Удаление происходит немедленно
- Можно удалить только ранее добавленные пути
- После удаления уведомления больше не поступают
- Не влияет на сами файлы и папки

---

<a id="monitor_list"></a>
### `/monitor_list` - Показать все активные мониторинги

**Использование:**
```
/monitor_list
```

**Отображаемая информация:**
- Полный путь к отслеживаемой папке/файлу
- Статус мониторинга (активен/неактивен)
- Количество отслеживаемых объектов

**Особенности:**
- Показывает все активные мониторинги
- Обновляется в реальном времени
- Включает системные и скрытые пути

---

<a id="monitor_stop"></a>
### `/monitor_stop` - Полная остановка всех мониторингов

**Использование:**
```
/monitor_stop
```

**Результат выполнения:**
- Немедленная остановка всех мониторингов
- Очистка списка отслеживаемых путей
- Прекращение всех уведомлений

**Восстановление работы:**
После остановки необходимо заново добавить нужные пути с помощью `/monitor_add`

⚠️ **Внимание:** Эта операция необратима и требует подтверждения

---

<a id="rdp_start"></a>
### `/rdp_start` - Запуск трансляции экрана

**Использование:**
```
/rdp_start - со стандартным FPS (1 кадр/сек)
/rdp_start [FPS] - с указанной частотой кадров
```

**Особенности:**
- Редактирует одно сообщение для экономии места
- Автоматически останавливается при неактивности
- Поддерживает 1-10 FPS

**Примеры:**
```
/rdp_start
/rdp_start 3
```

---

<a id="rdp_stop"></a>
### `/rdp_stop` - Остановка трансляции экрана

**Использование:**
```
/rdp_stop
```

---

## 📝 Примечания

- Все команды чувствительны к регистру
- Параметры в квадратных скобках `[]` являются необязательными
- Параметры в угловых скобках `<>` являются обязательными
- Для получения справки по конкретной команде используйте `/help [команда]`

## 🔒 Безопасность

⚠️ **Важно:** Используйте бота только на доверенных устройствах! Команды управления могут быть потенциально опасными.

## ⚖️ Правовое и этичное использование

**Бот предназначен для администрирования собственных машин.**

### ✅ Разрешенное использование
- **Личные устройства** в вашей собственности
- **Рабочие компьютеры** с явного разрешения работодателя
- **Семейные устройства** с согласия владельца
- **Образовательные цели** в контролируемой среде

### ❌ Запрещенное использование
- **Несанкционированный доступ** к любым системам без разрешения
- **Корпоративные сети** без письменного разрешения
- **Общественные компьютеры** без согласия владельца
- **Любая вредоносная деятельность** или несанкционированное наблюдение
- **Нарушение местных законов** или правил

### 🚨 Правовое предупреждение
- **Вы несете полную ответственность** за использование данного ПО
- **Несанкционированный доступ** к компьютерным системам незаконен в большинстве юрисдикций
- **Всегда получайте явное разрешение** перед использованием на любом устройстве
- **Уважайте конфиденциальность и безопасность** других
- **Используйте на свой страх и риск** - разработчики не несут ответственности

**Используя данное ПО, вы соглашаетесь использовать его этично и законно.**

---

<a id="logs_export"></a>
### `/logs_export` - Меню экспорта логов

**Использование:**
```
/logs_export
```

**Что показывает:**
- Статистику логов (общее количество, ошибки, предупреждения)
- Распределение по уровням логирования
- Статистику по логгерам
- Inline-клавиатуру для выбора формата экспорта

**Доступные форматы:**
- JSON - структурированный формат
- CSV - табличный формат для анализа
- XML - XML-формат
- TXT - текстовый формат

---

<a id="logs_export_json"></a>
### `/logs_export_json` - Экспорт логов в JSON

**Использование:**
```
/logs_export_json
```

**Что делает:**
- Экспортирует все логи в JSON формате
- Создает файл с временной меткой
- Отправляет файл в чат

**Особенности:**
- Полная структура данных логов
- Подходит для программной обработки
- Включает метаданные и дополнительную информацию

---

<a id="logs_export_csv"></a>
### `/logs_export_csv` - Экспорт логов в CSV

**Использование:**
```
/logs_export_csv
```

**Что делает:**
- Экспортирует логи в CSV формате
- Создает таблицу с колонками: timestamp, level, logger, message
- Отправляет файл в чат

**Особенности:**
- Подходит для анализа в Excel/Google Sheets
- Легко фильтруется и сортируется
- Компактный формат

---

<a id="logs_export_xml"></a>
### `/logs_export_xml` - Экспорт логов в XML

**Использование:**
```
/logs_export_xml
```

**Что делает:**
- Экспортирует логи в XML формате
- Создает структурированный XML-документ
- Отправляет файл в чат

**Особенности:**
- Иерархическая структура данных
- Подходит для интеграции с XML-системами
- Включает атрибуты и элементы

---

<a id="logs_export_txt"></a>
### `/logs_export_txt` - Экспорт логов в TXT

**Использование:**
```
/logs_export_txt
```

**Что делает:**
- Экспортирует логи в текстовом формате
- Создает читаемый текстовый файл
- Отправляет файл в чат

**Особенности:**
- Человеко-читаемый формат
- Простой для просмотра
- Легко обрабатывается текстовыми редакторами

---

<a id="logs_filter"></a>
### `/logs_filter` - Фильтрация логов

**Использование:**
```
/logs_filter [опции]
```

**Опции:**
- `--level ERROR` - только ошибки
- `--logger auth` - только логи авторизации
- `--hours 24` - за последние N часов
- `--limit 100` - максимум записей

**Примеры:**
```
/logs_filter --level ERROR --hours 1
/logs_filter --logger auth --limit 50
```

**Особенности:**
- Гибкая фильтрация по различным критериям
- Показывает последние 20 записей в сообщении
- Полные результаты доступны через экспорт

---

<a id="logs_cleanup"></a>
### `/logs_cleanup` - Очистка старых файлов экспорта

**Использование:**
```
/logs_cleanup [дни]
```

**Параметры:**
- **дни** - количество дней для хранения файлов (по умолчанию: 30)

**Примеры:**
```
/logs_cleanup
/logs_cleanup 7
```

**Что делает:**
- Удаляет файлы экспорта старше указанного количества дней
- Освобождает место на диске
- Показывает результат очистки

---

<a id="stats"></a>
### `/stats` - Общая статистика бота

**Использование:**
```
/stats
```

**Что показывает:**
- Общую статистику работы бота
- Количество выполненных команд
- Статистику пользователей
- Метрики производительности
- Краткую сводку по всем категориям

---

<a id="stats_commands"></a>
### `/stats_commands` - Статистика команд

**Использование:**
```
/stats_commands
```

**Что показывает:**
- Топ-10 самых используемых команд
- Количество выполнений каждой команды
- Процентное распределение команд
- Статистику успешности выполнения

---

<a id="stats_users"></a>
### `/stats_users` - Статистика пользователей

**Использование:**
```
/stats_users
```

**Что показывает:**
- Активность пользователей
- Количество команд на пользователя
- Время последней активности
- Статистику сессий

---

<a id="stats_performance"></a>
### `/stats_performance` - Метрики производительности

**Использование:**
```
/stats_performance
```

**Что показывает:**
- Среднее время выполнения команд
- Статистику ошибок
- Использование ресурсов
- Производительность системы

---

<a id="stats_patterns"></a>
### `/stats_patterns` - Паттерны использования

**Использование:**
```
/stats_patterns
```

**Что показывает:**
- Часы пиковой активности
- Дни недели с наибольшей активностью
- Паттерны использования команд
- Тренды активности

---

<a id="stats_audit"></a>
### `/stats_audit` - Аудит-трейлы

**Использование:**
```
/stats_audit
```

**Что показывает:**
- Последние действия пользователей
- Время выполнения операций
- Результаты выполнения
- Детали операций

---

<a id="stats_export"></a>
### `/stats_export` - Экспорт статистики

**Использование:**
```
/stats_export
```

**Что делает:**
- Экспортирует всю статистику в файл
- Создает подробный отчет
- Отправляет файл в чат

**Особенности:**
- Включает все категории статистики
- Структурированный формат данных
- Подходит для анализа и архивирования

---

*Последнее обновление: 2025-09-25*
