# Конфигурация TelegramControlBot

TelegramControlBot использует класс `Settings` для управления конфигурацией приложения. Все настройки могут быть заданы через переменные окружения или `.env` файл.

## Основные настройки

### Токены и аутентификация
- `TELEGRAM_BOT_TOKEN` — токен Telegram бота (обязательный)
- `ALLOWED_USER_IDS` — список разрешённых пользователей через запятую (обязательный)
- `TELEGRAM_API_ID` — Telegram API ID (опционально)
- `TELEGRAM_API_HASH` — Telegram API Hash (опционально)

### Логирование
- `LOG_LEVEL` — уровень логирования (по умолчанию: INFO)
  - Допустимые значения: `TRACE`, `DEBUG`, `INFO`, `WARNING`, `ERROR`, `CRITICAL`
  - При неверном значении автоматически используется `INFO`

### Пути к директориям
- `DEFAULT_PATHS_FILE` — файл системных путей (по умолчанию: `jsons/DEFAULT_PATHS.json`)
- `LOGS_DIRECTORY` — директория для логов (по умолчанию: `logs`)
- `DATA_DIRECTORY` — директория для метрик и статистики (по умолчанию: `data`)
- `EXPORTS_DIRECTORY` — директория для экспорта логов (по умолчанию: `exports`)

### Система
- `ENCODING` — кодировка (опционально)
  - Для Windows: автоматически устанавливается `cp1251`
  - Для Unix-систем: по умолчанию `utf-8`, можно переопределить через переменную окружения

## Использование в коде

```python
from app.config import get_settings, reload_settings

# Получение настроек (singleton)
settings = get_settings()

# Проверка пользователя
if settings.is_user_allowed(user_id):
    # пользователь разрешен

# Получение списка разрешенных пользователей
allowed_users = settings.get_allowed_user_ids()

# Получение кодировки для текущей системы
encoding = settings.get_encoding()

# Получение корневой директории проекта
project_root = settings.get_project_root()

# Перезагрузка настроек
reload_settings()
```

## Валидация настроек

Система автоматически валидирует:
- Наличие обязательного токена `TELEGRAM_BOT_TOKEN` (выбрасывает `ValueError` при отсутствии)
- Уровень логирования (только допустимые значения, иначе используется `INFO`)
- Кодировку (автоматическая установка `cp1251` для Windows)
- Формат списка пользователей (поддерживает запятую и точку с запятой как разделители)

Директории создаются автоматически при запуске приложения. Пути задаются переменными окружения (см. раздел «Пути к директориям» выше); при отсутствии переменных используются значения по умолчанию.

## Пример .env файла

```env
# ========================
#  ОБЯЗАТЕЛЬНЫЕ НАСТРОЙКИ
# ========================

TELEGRAM_BOT_TOKEN=your_bot_token_here
ALLOWED_USER_IDS=123456789,987654321

# ========================
#  ОПЦИОНАЛЬНЫЕ НАСТРОЙКИ
# ========================

# TELEGRAM_API_ID=your_api_id_here
# TELEGRAM_API_HASH=your_api_hash_here

# Логирование (по умолчанию: INFO)
# LOG_LEVEL=INFO

# Пути (по умолчанию указаны в разделе выше)
# DEFAULT_PATHS_FILE=jsons/DEFAULT_PATHS.json
# LOGS_DIRECTORY=logs
# DATA_DIRECTORY=data
# EXPORTS_DIRECTORY=exports

# Кодировка (опционально, для Unix-систем; Windows: cp866)
# ENCODING=utf-8
```

---

## 📄 Лицензия

Проект распространяется по лицензии [GNU GPL v3.0](../LICENSE).

Изначальная задумка: Nlan_Cat

Автор: Eklipti

---

Copyright (C) 2025-2026 Eklipti.